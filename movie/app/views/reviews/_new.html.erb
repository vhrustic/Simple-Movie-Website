<%= form_for [@film, @film.reviews.build] do |f| %>
	<p>
		<%= f.label :title %></br>
		<%= f.text_field :title %>
	</p>
	<p>
		<%= f.label :text %></br>
		<%= f.text_area :text %>
	</p>
	<p>
		<%= f.label :score %>
		<%= f.number_field :score, in: 1..10 %><br>
	</p>
	<%= f.submit %>
<% end %>